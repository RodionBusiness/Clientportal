export enum onboardingIdType {
  passport = 'passport',
  identity = 'national_identity_card',
  ukResidence = 'uk_biometric_residence_permit',
  voterId = 'voter_id',
  taxId = 'tax_id',
  driving = 'driving_licence',
  face = 'face',
}

export enum onboardingIdSide {
  front = 'front',
  back = 'back',
}
