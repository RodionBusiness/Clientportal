export interface IClientUserEnabled {
  c_entity_types: string[];
  document_enabled: boolean;
  enabled: boolean;
  fullname: string;
  roles: string[];
  transaction_enabled: boolean;
  client_email: string;
  client_person_id: number;
}
